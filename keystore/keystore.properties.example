# keystore.properties.example - DO NOT COMMIT real secrets
# Copy this file to the repo root as `keystore.properties` (or create one there)
# and replace the placeholder values with your real keystore values.

# Relative path from project root to the JKS file (example: keystore/upload-keystore.jks)
storeFile=keystore/upload-keystore.jks
# Password for the keystore (store password)
storePassword=YOUR_STORE_PASSWORD_HERE
# Key alias inside the keystore
keyAlias=YOUR_KEY_ALIAS_HERE
# Password for the key (can be same as storePassword)
keyPassword=YOUR_KEY_PASSWORD_HERE

