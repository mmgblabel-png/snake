<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow haptic feedback for game events -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Network permissions for ads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Advertising ID permission (recommended for AdMob) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:label="Snake"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:supportsRtl="true"
        android:theme="@style/Theme.SnakeCompose">
        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id" />

        <!-- UMP auto-reset on reinstall is default; no special provider needed. -->

        <activity
            android:name="com.mmgb.snake.PrivacyPolicyActivity"
            android:exported="false" />

        <activity
            android:name="com.mmgb.snake.MainActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:launchMode="singleTop"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboard|keyboardHidden|layoutDirection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
